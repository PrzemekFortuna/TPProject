<Page x:Class="TPProject.View.RecursionView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:TPProject.View"
      xmlns:viewModel="clr-namespace:TPProject.ViewModel"
      mc:Ignorable="d" 
      d:DesignHeight="300" d:DesignWidth="300"
      Title="RecursionView" Background="White">

    <Page.DataContext>
        <viewModel:TreeViewViewModel/>
    </Page.DataContext>
    
    <Grid>
        <TreeView ItemsSource="{Binding FirstStudents}">
            <TreeView.ItemContainerStyle>
                <Style TargetType="{x:Type TreeViewItem}">
                    <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}"/>
                </Style>
            </TreeView.ItemContainerStyle>
            
            <TreeView.ItemTemplate>
                <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                    <TextBlock Text="{Binding Name}"/>
                </HierarchicalDataTemplate>
            </TreeView.ItemTemplate>
        </TreeView>

        <Button x:Name="serializeBtn" Content="Serialize" Width="120" Height="50" FontSize="24" VerticalAlignment="Bottom" HorizontalAlignment="Right" Margin="15" Command="{Binding SerializeCommand}"/>
    </Grid>
</Page>
